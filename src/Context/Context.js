import React from 'react'
import { createContext, useContext } from 'react'

const productcontexts=createContext()

//  export const contextsprovider=contexts.Provider
 export default productcontexts;
